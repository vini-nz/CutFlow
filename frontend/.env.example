# A SPA fala com a API na MESMA origem (proxy do Vite), então a base é relativa.
VITE_API_URL=/api/v1

# Alvo do proxy do dev server para o backend (ver vite.config.js).
# Em docker-compose isto é definido como http://backend:8080.
VITE_PROXY_TARGET=http://localhost:8080
